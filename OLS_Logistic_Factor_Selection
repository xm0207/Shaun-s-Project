##Preparation

```{r libraries}
library(readxl)
library(stats)
library(combinat)
library(dplyr)
```
```{r load data}
file_path <- "your_data_path"
data <- read_excel(file_path)
flitered_data<-data%>%select(columna,columnb...)
```
##Now we transform the indicator and try to run regression

```{r}
Dep_Var
